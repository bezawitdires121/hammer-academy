"use server";

// Temporary placeholder — being rebuilt for class-level batch publishing
// (Step 4 of the current redesign). Do not use until rebuilt.

export async function publishResult() {
  return { error: "Not yet rebuilt for the new result card system." };
}

export async function unpublishResult() {
  return { error: "Not yet rebuilt for the new result card system." };
}