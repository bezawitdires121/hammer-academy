"use server";

// Temporary placeholder — being rebuilt for class-level multi-subject
// result entry (Step 2 of the current redesign). Do not use until rebuilt.

export async function createResult() {
  return { error: "Not yet rebuilt for the new result card system." };
}